server:
  port: 10010 # 网关端口
spring:
  application:
    name: gateway # 服务名称
  cloud:
    nacos:
      server-addr: localhost:8848 # nacos地址
    gateway:
      routes: # 网关路由配置
        - id: pdf2epub-Service
          uri: lb://pdf-epub-service #uri: lb://pdf2epubService
          predicates:
            - Path=/PdfToEpub/**
      default-filters:
        - AddRequestHeader=Truth, ZJW is freaking awesome! # 添加请求头

#      globalcors: # 全局的跨域处理
#        add-to-simple-url-handler-mapping: true # 解决options请求被拦截问题
#        corsConfigurations:
#          '[/**]':
#            allowedOrigins: "*"  # 允许哪些网站的跨域请求 allowedOrigins: “*” 允许所有网站跨域
#            allowedMethods: # 允许的跨域ajax的请求方式
#              - "GET"
#              - "POST"
#              - "DELETE"
#              - "PUT"
#              - "OPTIONS"
#            allowedHeaders: "*" # 允许在请求中携带的头信息
#            allowCredentials: true # 是否允许携带cookie
#            maxAge: 360000 # 这次跨域检测的有效期